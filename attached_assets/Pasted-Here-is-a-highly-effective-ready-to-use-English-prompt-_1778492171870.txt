Here is a highly effective, ready-to-use English prompt you can copy-paste into Grok, Claude, GPT-4o, or any strong coding model:

---

Prompt:

You are an expert in computer vision, steganography, and AI-generated image forensics.

Create a complete, well-documented Python script that can detect invisible pixel-level watermarks (SynthID-style or OpenAI-style) embedded in images generated by modern AI models like Gemini (Imagen), DALL·E 3 / GPT Image, Midjourney, etc.

The script must include the following features:

1. Template Extraction Mode  
   - Take a folder containing 50–200+ fully black (or white) images generated by the same AI model.  
   - Compute the average pixel-wise watermark template.

2. Detection Mode (main function)  
   - Load any test image (JPEG, PNG, WEBP).  
   - Support detection using:
     - Frequency domain analysis (2D FFT + fftshift)
     - Phase coherence checking against the template
     - Magnitude spectrum anomaly detection
     - Cross-correlation in spatial and frequency domains
     - Optional: saturation enhancement visualization (HSV) to make the watermark visible to humans

3. Additional robust methods  
   - C2PA metadata inspection (using c2pa or exiftool logic)
   - LSB (Least Significant Bits) pattern analysis
   - Multi-scale / multi-resolution support (different image sizes)

4. Output  
   - Watermark confidence score (0.0 – 1.0)
   - Clear verdict: "AI-generated with high confidence", "Likely real", "Inconclusive"
   - Visual debug images: enhanced saturation version, FFT magnitude spectrum, extracted watermark overlay

Requirements:
- Use only numpy, scipy, opencv-python, pillow (and c2pa if possible)
- Add detailed comments and explanations
- Make the code modular and easy to extend
- Include example usage for both template creation and detection
- Handle different image resolutions gracefully

Write the full code with a clean CLI interface using argparse. Also provide a short README section at the top of the file explaining how to use it.

Start your response with the complete Python script.

---

How to use this prompt:

1. Paste it directly into ChatGPT / Claude / Grok.
2. If you want a more advanced version, add at the end:
   > "Also implement a deep learning version using a small CNN trained on watermark vs non-watermark patches."

3. If you want only the frequency-domain part (fastest and most reliable):
   > "Focus primarily on the FFT + phase coherence method, as it is currently the most effective against SynthID and similar watermarks."

Would you like me to also create a shorter version (just the core FFT detector) or a maximum strength version (with neural net + ensemble)?